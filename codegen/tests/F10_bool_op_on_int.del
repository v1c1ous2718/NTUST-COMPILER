(* FAIL — §2.3.1 Expressions / Boolean
 * and / or / not require boolean operands. *)
program BoolOpOnInt;
var a : integer = 1;
var b : integer = 0;
begin
    writeln a and b;            (* and on integers *)
end BoolOpOnInt.
