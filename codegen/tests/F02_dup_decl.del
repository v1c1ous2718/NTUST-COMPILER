(* FAIL — §2.1 Global Variables
 * an identifier may not be declared twice in the same scope. *)
program DupDecl;
var x : integer;
var x : integer;        (* duplicate of `x` *)
begin
    x := 1;
end DupDecl.
