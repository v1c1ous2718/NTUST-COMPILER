{ §2.1.2 vars — id_list groups same-typed identifiers; each line starts with `var`. }
program vars;
var s : string;
var i : integer = 100;
var d : real;
var b : boolean;
var aa, bb, cc : integer;
var pp, qq : real = 0.0;
begin
end vars.
